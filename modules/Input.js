import React from 'react';
import PropType from 'prop-types';

const Input = props =>
  <form onSubmit={this.preventSubmit}>
     <input type='text' placeholder=' Enter City...' onKeyDown=
     {props.queryWeather}/>
     </form>

     export default Input;